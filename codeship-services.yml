dmk-python2.7:
  add_docker: true
  working_dir: /opt/DockerMake/test
  build:
    dockerfile: Dockerfile
    context: .
    args:
      PYVERSION: "2.7"

dmk-python3.5:
  add_docker: true
  working_dir: /opt/DockerMake/test
  build:
    dockerfile: Dockerfile
    context: .
    args:
      PYVERSION: "3.5"

dmk-python3.6:
  add_docker: true
  working_dir: /opt/DockerMake/test
  build:
    dockerfile: Dockerfile
    context: .
    args:
      PYVERSION: "3.6"

avirshup/docker-make:  # this is the deployment artifact
  build:
    dockerfile: Dockerfile
    context: .
    args:
      PYVERSION: "3.6"


dmk-deploy-img:
  add_docker: true
  build:
    context: deployment/
    dockerfile: Dockerfile.deploy
  encrypted_env_file: deployment/tokens.crypt
  encrypted_docker_cfg: deployment/dockercfg.crypt
  volumes:
    - "./codeship-distfiles:/opt/distfiles"
