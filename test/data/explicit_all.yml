_ALL_:
  - t1
  - t3

_SOURCES_:
   - included_all.yml

t1:
  FROM: alpine
  build: |
    RUN mkdir -p /opt && echo t1 > /opt/t1

t2:
  FROM: alpine
  build: |
    RUN mkdir -p /opt && echo t2 > /opt/t2
