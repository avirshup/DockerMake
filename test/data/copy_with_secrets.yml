copy_with_secrets:
  description: Parsing fails because squash and copy_files don't play together yet
  FROM: whatever
  secret_files:
    - whatever
  copy_from:
    whatever:
      whatever: whatever
  build: whatever
